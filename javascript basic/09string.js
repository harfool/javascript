// string  

// use string new way to declear string avoid old way

name = `harfool gujjar`
age = 21
skill = `full stack web developer `
user = `my name is ${name} i am ${age} old my skill is ${skill}`
//console.log(user);

//string methods 
str = `my name is  harfool`
console.log(str.length) //The length property returns the length of a string:
console.log(str.trim()); //The trim() method removes whitespace from both sides of a string:
console.log(str.concate(user))